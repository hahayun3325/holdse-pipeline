checkpoint:
  monitor: "train_loss"
  mode: "min"
  save_top_k: 3
  every_n_epochs: 2
  save_last: true
  dirpath: "~/Projects/holdse/code/logs/outputs/${experiment.name}/checkpoints"
  filename: "epoch{epoch:02d}-loss{train_loss:.2f}"
  verbose: true
